on: 
  workflow_dispatch:
  push:
    paths:
      - config/**
      - boards/**
      - build.yaml
 
jobs:
  build:
    # 今はこうなっているはず
uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
